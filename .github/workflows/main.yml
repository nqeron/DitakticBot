on:
  push:
    branches:
      - main
      
jobs:

  build_debug_windows:
    name: build debug for windows
    runs-on: windows-latest
    
    steps:
      - name: Setup Nim environment
        uses: iffy/install-nim@v4.5.0
      
      - name: builds to bin/debug/
        run: nimble build
